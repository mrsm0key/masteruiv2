const contractAddresses = require("./contractAddresses.json")
const abi = require("./abi.json")
const contractAddressesStack = require("./contractAddressesStack.json")
const abiStack = require("./abiStack.json")

module.exports = {
    contractAddresses,
    abi,
    contractAddressesStack,
    abiStack,
}
